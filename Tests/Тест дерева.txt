#include <iostream>
#include <vector>
#include <fstream>
#include <string>
#include <sstream>
#include <map>
#include <queue>
#include "SaleTree.h"
#include "CircularList.h"

using namespace std;

// Структура для представления узла дерева при визуализации
struct TreeNode {
    int key;
    string indices;
    TreeNode* left;
    TreeNode* right;
    TreeNode* parent;

    TreeNode(int k, const string& idx) : key(k), indices(idx), left(nullptr), right(nullptr), parent(nullptr) {}
};

// Функция для построения дерева из данных SaleTree
TreeNode* buildTreeFromSaleTree(const SaleTree& tree) {
    // Экспортируем дерево во временный файл
    if (!tree.exportToFile("temp_tree_structure.txt")) {
        return nullptr;
    }

    // Читаем структуру дерева
    ifstream file("temp_tree_structure.txt");
    if (!file.is_open()) {
        return nullptr;
    }

    vector<pair<int, string>> preOrderData;
    string line;
    while (getline(file, line)) {
        if (line.empty()) continue;

        size_t colonPos = line.find(':');
        if (colonPos != string::npos) {
            int key = stoi(line.substr(0, colonPos));
            string indices = line.substr(colonPos + 2);
            preOrderData.push_back({key, indices});
        }
    }
    file.close();
    remove("temp_tree_structure.txt");

    if (preOrderData.empty()) {
        return nullptr;
    }

    // Создаем узлы
    map<int, TreeNode*> nodeMap;
    for (const auto& data : preOrderData) {
        nodeMap[data.first] = new TreeNode(data.first, data.second);
    }

    // Восстанавливаем структуру дерева на основе BST свойств
    TreeNode* root = nodeMap[preOrderData[0].first];

    for (size_t i = 1; i < preOrderData.size(); i++) {
        int key = preOrderData[i].first;
        TreeNode* newNode = nodeMap[key];
        TreeNode* current = root;

        while (true) {
            if (key < current->key) {
                if (!current->left) {
                    current->left = newNode;
                    newNode->parent = current;
                    break;
                } else {
                    current = current->left;
                }
            } else {
                if (!current->right) {
                    current->right = newNode;
                    newNode->parent = current;
                    break;
                } else {
                    current = current->right;
                }
            }
        }
    }

    return root;
}

// Рекурсивная функция для красивого вывода дерева с указанием предков
void printTreeWithParents(TreeNode* node, const string& prefix, bool isLast, const vector<SaleEntry>& sales, bool isRoot = false) {
    if (!node) return;

    if (isRoot) {
        cout << "🌳 КОРЕНЬ: " << node->key << " [" << node->indices << "]" << endl;
    } else {
        cout << prefix;
        cout << (isLast ? "└── " : "├── ");

        // Определяем, левый или правый ребенок
        string childType = (node->parent && node->parent->left == node) ? "ЛЕВЫЙ" : "ПРАВЫЙ";
        cout << childType << ": " << node->key << " [" << node->indices << "]";

        // Добавляем информацию о предке
        if (node->parent) {
            cout << " ПРЕДОК " << node->parent->key;
        }
        cout << endl;
    }

    // Показываем записи для текущего узла
    string detailPrefix = prefix;
    if (!isRoot) {
        detailPrefix += (isLast ? "    " : "│   ");
    }
    cout << detailPrefix << "└─ Записи:" << endl;

    // Парсим индексы и показываем соответствующие записи
    stringstream ss(node->indices);
    string token;
    vector<int> indices;

    while (getline(ss, token, ' ')) {
        if (token == "->" || token.empty()) continue;
        try {
            indices.push_back(stoi(token));
        } catch (...) {
            // Игнорируем некорректные значения
        }
    }

    for (int idx : indices) {
        if (idx < sales.size()) {
            cout << detailPrefix << "   • Запись " << idx << ": "
                 << sales[idx].quantity << " шт., "
                 << sales[idx].cashier << ", "
                 << sales[idx].date << endl;
        }
    }

    // Подготавливаем префикс для детей
    string childPrefix = prefix;
    if (!isRoot) {
        childPrefix += (isLast ? "    " : "│   ");
    }

    // Определяем, есть ли дети
    bool hasLeft = (node->left != nullptr);
    bool hasRight = (node->right != nullptr);

    // Выводим детей (сначала левый, потом правый)
    if (hasLeft && hasRight) {
        // Есть оба ребенка
        printTreeWithParents(node->left, childPrefix, false, sales);
        printTreeWithParents(node->right, childPrefix, true, sales);
    } else if (hasLeft) {
        // Только левый ребенок
        printTreeWithParents(node->left, childPrefix, true, sales);
    } else if (hasRight) {
        // Только правый ребенок
        printTreeWithParents(node->right, childPrefix, true, sales);
    }
}

// Освобождение памяти дерева
void deleteTree(TreeNode* node) {
    if (!node) return;
    deleteTree(node->left);
    deleteTree(node->right);
    delete node;
}

// Улучшенная функция для визуализации AVL-дерева
void printBeautifulTree(const SaleTree& tree, const vector<SaleEntry>& sales) {
    cout << "\n┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓\n";
    cout << "┃                    СТРУКТУРА AVL-ДЕРЕВА                  ┃\n";
    cout << "┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛\n";

    TreeNode* root = buildTreeFromSaleTree(tree);
    if (!root) {
        cout << "\n🌿 Дерево пустое\n";
        return;
    }

    cout << "\nСтруктура дерева (формат: Позиция: Артикул [индексы_записей] ПРЕДОК предок):\n";
    cout << "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n";

    printTreeWithParents(root, "", true, sales, true);

    // Подсчитываем статистику
    int nodeCount = 0;
    queue<TreeNode*> q;
    q.push(root);
    while (!q.empty()) {
        TreeNode* current = q.front();
        q.pop();
        nodeCount++;
        if (current->left) q.push(current->left);
        if (current->right) q.push(current->right);
    }

    cout << "\n📊 Статистика дерева:\n";
    cout << "   • Количество узлов: " << nodeCount << "\n";
    cout << "   • Общее количество записей: " << sales.size() << "\n";

    deleteTree(root);
}

// Функция для создания тестового файла
void createTestFile() {
    ofstream file("test_sales.txt");
    file << "1001 5 Иванов;15 январь 2024\n";
    file << "1002 3 Петров;16 январь 2024\n";
    file << "1001 2 Сидоров;17 январь 2024\n";
    file << "1003 7 Иванов;18 январь 2024\n";
    file << "1002 1 Козлов;19 январь 2024\n";
    file << "1001 4 Петров;20 январь 2024\n";
    file << "1004 8 Сидоров;21 январь 2024\n";
    file << "1003 2 Иванов;22 январь 2024\n";
    file.close();
}

// Функция для вывода записей продаж
void printSales(const vector<SaleEntry>& sales) {
    cout << "\n┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓\n";
    cout << "┃                      ЗАПИСИ ПРОДАЖ                       ┃\n";
    cout << "┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛\n";
    for (size_t i = 0; i < sales.size(); i++) {
        cout << "📦 Запись " << i << ": Артикул " << sales[i].article
             << ", Количество: " << sales[i].quantity << " шт."
             << ", Кассир: " << sales[i].cashier
             << ", Дата: " << sales[i].date << endl;
    }
}

// Функция для поиска и вывода результатов
void searchAndPrint(const SaleTree& tree, const vector<SaleEntry>& sales, int article) {
    cout << "\n🔍 Поиск артикула " << article << "\n";
    cout << "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n";

    CircularList indices = tree.search(article);

    if (indices.size() == 0) {
        cout << "❌ Артикул " << article << " не найден.\n";
        return;
    }

    cout << "✅ Найдено " << indices.size() << " записей:\n";

    // Показываем все записи с данным артикулом
    int count = 0;
    for (size_t j = 0; j < sales.size(); j++) {
        if (sales[j].article == article) {
            count++;
            cout << "   " << count << ". 📦 Запись " << j << ": "
                 << sales[j].quantity << " шт., кассир "
                 << sales[j].cashier << ", дата "
                 << sales[j].date << "\n";
        }
    }
}

// Функция для интерактивного тестирования
void interactiveTest(SaleTree& tree, vector<SaleEntry>& sales) {
    int choice;
    do {
        cout << "\n┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓\n";
        cout << "┃                   МЕНЮ ТЕСТИРОВАНИЯ                      ┃\n";
        cout << "┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛\n";
        cout << "1. 🌳 Показать структуру дерева\n";
        cout << "2. 🔍 Поиск по артикулу\n";
        cout << "3. ➕ Добавить запись\n";
        cout << "4. ❌ Удалить запись\n";
        cout << "5. 📋 Показать все записи продаж\n";
        cout << "6. 💾 Экспорт дерева в файл\n";
        cout << "7. 🗑️  Очистить дерево\n";
        cout << "0. 🚪 Выход\n";
        cout << "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n";
        cout << "Выберите опцию: ";
        cin >> choice;

        switch (choice) {
            case 1:
                printBeautifulTree(tree, sales);
                break;

            case 2: {
                int article;
                cout << "Введите артикул для поиска: ";
                cin >> article;
                searchAndPrint(tree, sales, article);
                break;
            }

            case 3: {
                SaleEntry newEntry;
                cout << "Введите артикул: ";
                cin >> newEntry.article;
                cout << "Введите количество: ";
                cin >> newEntry.quantity;
                cout << "Введите кассира: ";
                cin.ignore();
                getline(cin, newEntry.cashier);
                cout << "Введите дату: ";
                getline(cin, newEntry.date);

                sales.push_back(newEntry);
                tree.add(newEntry.article, sales.size() - 1);
                cout << "✅ Запись добавлена с индексом " << (sales.size() - 1) << endl;
                break;
            }

            case 4: {
                int article, index;
                cout << "Введите артикул для удаления: ";
                cin >> article;
                cout << "Введите индекс записи (-1 для удаления всех): ";
                cin >> index;
                tree.remove(article, index);
                cout << "✅ Запись удалена.\n";
                break;
            }

            case 5:
                printSales(sales);
                break;

            case 6: {
                string filename;
                cout << "Введите имя файла для экспорта: ";
                cin >> filename;
                if (tree.exportToFile(filename)) {
                    cout << "✅ Дерево экспортировано в файл " << filename << endl;
                } else {
                    cout << "❌ Ошибка при экспорте в файл.\n";
                }
                break;
            }

            case 7:
                tree.clear();
                sales.clear();
                cout << "✅ Дерево и записи очищены.\n";
                break;

            case 0:
                cout << "🚪 Выход из программы.\n";
                break;

            default:
                cout << "❌ Неверный выбор. Попробуйте снова.\n";
        }
    } while (choice != 0);
}

// Автоматические тесты
void runAutomaticTests() {
    cout << "\n┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓\n";
    cout << "┃                  АВТОМАТИЧЕСКИЕ ТЕСТЫ                    ┃\n";
    cout << "┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛\n";

    // Тест 1: Создание и базовые операции
    cout << "\n🧪 Тест 1: Создание дерева и добавление элементов\n";
    SaleTree testTree;
    vector<SaleEntry> testSales;

    // Добавляем тестовые данные
    SaleEntry e1 = {1001, 5, "Иванов", "15 январь 2024"};
    SaleEntry e2 = {1002, 3, "Петров", "16 январь 2024"};
    SaleEntry e3 = {1001, 2, "Сидоров", "17 январь 2024"};

    testSales.push_back(e1);
    testSales.push_back(e2);
    testSales.push_back(e3);

    testTree.add(1001, 0);
    testTree.add(1002, 1);
    testTree.add(1001, 2);

    cout << "\nДерево после добавления:\n";
    testTree.print(cout);

    // Тест 2: Поиск
    cout << "\n🧪 Тест 2: Поиск элементов\n";
    CircularList result1001 = testTree.search(1001);
    CircularList result1002 = testTree.search(1002);
    CircularList result9999 = testTree.search(9999);

    cout << "✅ Поиск 1001: найдено " << result1001.size() << " записей\n";
    cout << "✅ Поиск 1002: найдено " << result1002.size() << " записей\n";
    cout << "❌ Поиск 9999: найдено " << result9999.size() << " записей\n";

    // Тест 3: Удаление
    cout << "\n🧪 Тест 3: Удаление элементов\n";
    testTree.remove(1001, 0);  // Удаляем конкретную запись
    cout << "После удаления записи 1001 с индексом 0:\n";
    testTree.print(cout);

    testTree.remove(1002, -1); // Удаляем все записи с артикулом 1002
    cout << "После удаления всех записей 1002:\n";
    testTree.print(cout);

    cout << "\n✅ Автоматические тесты завершены.\n";
}

int main() {
    system("chcp 65001");
    cout << "┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓\n";
    cout << "┃                 ТЕСТИРОВАНИЕ ДЕРЕВА ПРОДАЖ               ┃\n";
    cout << "┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛\n";

    // Создаем тестовый файл
    createTestFile();
    cout << "📁 Создан тестовый файл 'test_sales.txt'\n";

    // Создаем дерево и загружаем данные
    SaleTree tree;
    vector<SaleEntry> sales;

    cout << "\n📥 Загрузка данных из файла...\n";
    if (tree.importFromFile("test_sales.txt", sales)) {
        cout << "✅ Данные успешно загружены! Количество записей: " << sales.size() << endl;

        // Показываем загруженные данные
        printSales(sales);

        // Показываем структуру дерева
        printBeautifulTree(tree, sales);

        // Демонстрация поиска
        cout << "\n┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓\n";
        cout << "┃                   ДЕМОНСТРАЦИЯ ПОИСКА                    ┃\n";
        cout << "┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛\n";
        searchAndPrint(tree, sales, 1001);
        searchAndPrint(tree, sales, 1002);
        searchAndPrint(tree, sales, 1003);
        searchAndPrint(tree, sales, 1004);
        searchAndPrint(tree, sales, 9999); // Несуществующий артикул

        // Запускаем автоматические тесты
        runAutomaticTests();

        // Интерактивное тестирование
        char choice;
        cout << "\n❓ Запустить интерактивное тестирование? (y/n): ";
        cin >> choice;
        if (choice == 'y' || choice == 'Y') {
            interactiveTest(tree, sales);
        }

    } else {
        cout << "❌ Ошибка при загрузке данных из файла!\n";
        return 1;
    }

    cout << "\n🏁 Программа завершена.\n";
    return 0;
}